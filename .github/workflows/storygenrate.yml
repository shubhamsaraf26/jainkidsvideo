name: Daily Jain Story

on:
  schedule:
    - cron: '30 3 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  story:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - run: |
          pip install openai

      - run: |
          python scripts/generate_jain_story_gpt.py
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

      - run: |
          git config user.name github-actions
          git config user.email github-actions@github.com

          git add stories/

          git commit -m "Daily Jain Story $(date +'%Y-%m-%d')" || exit 0

          git push